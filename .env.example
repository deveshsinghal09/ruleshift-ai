# Configure these only in a local .env.local file or encrypted server-side
# environment settings. Never expose credentials to the browser.
GEMINI_API_KEY=
GEMINI_MODEL=
DATABASE_URL=
TEST_DATABASE_URL=
